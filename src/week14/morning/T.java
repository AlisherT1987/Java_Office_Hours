package week14.morning;

public interface T {
    public static final int K = 1; // will show dark gray because redundant keywords
    public abstract void p();

    boolean HAS_BATTERIES = true;
    void test(); // since public abstract redundant we did not write it
}
