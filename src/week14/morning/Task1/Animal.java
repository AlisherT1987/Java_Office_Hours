package week14.morning.Task1;

public interface Animal {
    void move(); // since other concrete child classes are going to override this method, we can create an abstract method like this, so we can focus what is essential at child methods
}
